export enum Categories {
  'Giftcard' = 1,
  'Physical' = 2,
  'Credit' = 3,
}

export enum Deliveries {
  'ECard' = 1,
  'Shipping' = 2,
  'Store Credit' = 3,
}

export enum StatusCode {
  Unchanged,
  Added,
  Deleted,
  Updated,
  Reset,
}
